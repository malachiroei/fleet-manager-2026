# Stable KV reference (2026-03-21)

This folder holds a **documented reference** for `system_settings` rows after selective publish was verified:

- **`version_manifest`:** Star-only UI line in `changes` (Bold not published to Pro yet).
- **`pending_changes`:** Bold line queued for **2.7.9** (or next publish).

> **Not a secret:** Replace dates/versions if your live DB differs. To snapshot **live** Supabase data, export from the dashboard or run a one-off query and save the JSON outside the repo if it contains anything sensitive.

## Files

- `kv-reference.json` — example `value` payloads for the two keys (structure only).
